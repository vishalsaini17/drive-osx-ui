# Base path for the platform API.
# In Docker the UI is served behind nginx, which proxies /api/v1 to the API
# container, so a relative path is correct in every environment.
VITE_API_BASE_URL="/api/v1"
