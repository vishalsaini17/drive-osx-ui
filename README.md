
1. Install dependencies:
   `npm install`
2. Optionally override `VITE_API_BASE_URL` in `.env` — see [.env.example](.env.example)
   for the documented default. Keep the two in step (root `CLAUDE.md` §35).
3. Run the app:
   `npm run dev`
